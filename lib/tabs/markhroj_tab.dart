import 'package:flutter/material.dart';

class MarkhrojTab extends StatelessWidget {
  const MarkhrojTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
